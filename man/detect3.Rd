% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/detect3.R
\name{detect3}
\alias{detect3}
\title{All-in-one marine heatwave detection for gridded data}
\usage{
detect3(
  file_in,
  name,
  climatologyPeriod,
  var_name = NULL,
  lon_range = NULL,
  lat_range = NULL,
  time_range = NULL,
  depth = NULL,
  maxPadLength = FALSE,
  windowHalfWidth = 5L,
  pctile = 90,
  smoothPercentile = TRUE,
  smoothPercentileWidth = 31L,
  detrend = FALSE,
  minDuration = 5L,
  minDuration2 = minDuration,
  joinAcrossGaps = TRUE,
  maxGap = 2L,
  maxGap2 = maxGap,
  threshClim2 = NULL,
  threshClim2_var_name = NULL,
  coldSpells = FALSE,
  category = FALSE,
  hemisphere = "south",
  roundClm = 4L,
  roundRes = 4L,
  daily = c("none", "also", "only"),
  protoEvent = FALSE,
  n_threads = 1L,
  skip_bad_files = FALSE,
  quiet = FALSE
)
}
\arguments{
\item{file_in}{Path to input NetCDF file (or directory/vector of daily files).}

\item{name}{Output base name (a path stem) shared by both stages. Produces
\code{paste0(name, "_clim.nc")} and \code{paste0(name, "_events.nc")} (plus
the per-day products; see \code{daily} and \code{protoEvent}). The
directory must exist.}

\item{climatologyPeriod}{Character vector of length 2 with start and end
dates of the baseline period, for example \code{c("1991-01-01", "2020-12-31")}.}

\item{var_name}{Name of the SST variable. If \code{NULL}, auto-detected.}

\item{lon_range}{Optional \code{c(min, max)} longitude range.}

\item{lat_range}{Optional \code{c(min, max)} latitude range.}

\item{time_range}{Optional \code{c("start", "end")} date range.}

\item{depth}{Optional depth/level index for 4D data.}

\item{maxPadLength}{Max consecutive NAs to interpolate. Default \code{FALSE}.}

\item{windowHalfWidth}{Half-width of climatology window. Default \code{5}.}

\item{pctile}{Percentile for threshold. Default \code{90} (heatwaves);
use \code{10} for cold-spells.}

\item{smoothPercentile}{Apply rolling mean smoothing? Default \code{TRUE}.}

\item{smoothPercentileWidth}{Smoothing window width. Default \code{31}.}

\item{detrend}{Logical. Remove a linear trend before climatology calculation?
Default \code{FALSE}.}

\item{minDuration}{Minimum event duration in days. Default \code{5}.}

\item{minDuration2}{Secondary minimum duration. See
\code{\link{detect_event3}}.}

\item{joinAcrossGaps}{Join events across short gaps? Default \code{TRUE}.}

\item{maxGap}{Maximum gap length to join. Default \code{2}.}

\item{maxGap2}{Secondary maximum gap length. See
\code{\link{detect_event3}}.}

\item{threshClim2}{Optional gridded NetCDF logical criterion for secondary
event detection. See \code{\link{detect_event3}}.}

\item{threshClim2_var_name}{Name of the \code{threshClim2} variable. If
\code{NULL}, it is auto-detected.}

\item{coldSpells}{Detect cold-spells? Default \code{FALSE}.}

\item{category}{Logical. Compute Hobday et al. (2018) severity categories
inline? Default \code{FALSE}.}

\item{hemisphere}{Character. \code{"south"} (default) or \code{"north"}.}

\item{roundClm}{Decimal places for climatology rounding. Default \code{4}.}

\item{roundRes}{Decimal places for event metric rounding. Default \code{4}.}

\item{daily}{Per-day output control: \code{"none"} (default), \code{"also"},
or \code{"only"}. See \code{\link{detect_event3}}.}

\item{protoEvent}{Logical. Write the per-day proto-event series
(\code{paste0(name, "_protoevents.nc")}) instead of the event table. See
\code{\link{detect_event3}}. Default \code{FALSE}.}

\item{n_threads}{Number of OpenMP threads. Default \code{1}.}

\item{skip_bad_files}{Logical. For multi-file inputs, skip unreadable files
or files with mismatched grids instead of failing. Default \code{FALSE}.}

\item{quiet}{Logical. Suppress the post-computation console summaries?
Default \code{FALSE}.}
}
\value{
Invisibly returns a named character vector of the NetCDF files
written (\code{clim} plus a subset of \code{events}, \code{daily},
\code{protoevents}). Use \code{\link{hw3_export}} to read or export any of
them.
}
\description{
The primary entry point for \code{heatwave3}. Runs
\code{\link{ts2clm3}} followed by \code{\link{detect_event3}} in a
single call, with optional inline category computation.
}
\examples{
\donttest{
sst_file <- system.file("extdata/sst_test.nc", package = "heatwave3")

detect3(sst_file, name = file.path(tempdir(), "demo"),
        climatologyPeriod = c("1982-01-01", "2011-12-31"))
}
}
