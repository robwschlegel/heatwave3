% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/exceedance3.R
\name{exceedance3}
\alias{exceedance3}
\title{Static threshold exceedance detection for gridded data}
\usage{
exceedance3(
  file_in,
  file_out,
  threshold,
  var_name = NULL,
  below = FALSE,
  minDuration = 5L,
  joinAcrossGaps = TRUE,
  maxGap = 2L,
  lon_range = NULL,
  lat_range = NULL,
  time_range = NULL,
  depth = NULL,
  roundRes = 4L,
  n_threads = 1L
)
}
\arguments{
\item{file_in}{Path to input NetCDF file.}

\item{file_out}{Path for output event NetCDF file.}

\item{threshold}{The static threshold value.}

\item{var_name}{SST variable name. If \code{NULL}, auto-detected.}

\item{below}{Logical. If \code{TRUE}, detect exceedances below the threshold.
Default \code{FALSE}.}

\item{minDuration}{Minimum event duration in days. Default \code{5}.}

\item{joinAcrossGaps}{Join events across short gaps? Default \code{TRUE}.}

\item{maxGap}{Maximum gap to join. Default \code{2}.}

\item{lon_range}{Optional \code{c(min, max)} longitude range.}

\item{lat_range}{Optional \code{c(min, max)} latitude range.}

\item{time_range}{Optional \code{c("start", "end")} date range.}

\item{depth}{Optional depth index.}

\item{roundRes}{Decimal places for rounding. Default \code{4}.}

\item{n_threads}{Number of threads for parallel computation. Default \code{1}.}
}
\value{
Invisibly returns the output file path.
}
\description{
Detects periods where SST exceeds (or falls below) a fixed threshold
for each pixel, without requiring a climatology baseline.
}
\examples{
\dontrun{
sst_file <- system.file("extdata/sst_test.nc", package = "heatwave3")
event_file <- tempfile(fileext = ".nc")

# Detect periods where SST exceeds 295 K
exceedance3(sst_file, event_file, threshold = 295)
}
}
