% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/geom_lolli3.R
\docType{data}
\name{geom_lolli3}
\alias{geom_lolli3}
\alias{GeomLolli3}
\title{Lollipop geom for event metric visualisation}
\usage{
geom_lolli3(
  mapping = NULL,
  data = NULL,
  stat = "identity",
  position = "identity",
  ...,
  na.rm = FALSE,
  show.legend = NA,
  inherit.aes = TRUE
)
}
\arguments{
\item{mapping}{Aesthetic mappings. Requires \code{x} and \code{y}.}

\item{data}{The data to display.}

\item{stat}{Statistical transformation. Default \code{"identity"}.}

\item{position}{Position adjustment. Default \code{"identity"}.}

\item{...}{Additional arguments.}

\item{na.rm}{Remove NAs? Default \code{FALSE}.}

\item{show.legend}{Show legend? Default \code{NA}.}

\item{inherit.aes}{Inherit aesthetics? Default \code{TRUE}.}
}
\value{
A ggplot2 layer.
}
\description{
A ggplot2 geom that draws lollipop (stem + point) plots for event metrics.
}
\details{
Each lollipop is drawn independently from its own row -- unlike
\code{\link{geom_flame3}}, nothing here depends on row order or grouping
within a series. For depth-resolved data, map \code{depth} to \code{colour}
(or \code{shape}) to distinguish levels, or use
\code{ggplot2::facet_wrap(~ depth)}; both work with the standard ggplot2
aesthetics and need no special handling from this geom.
}
\examples{
library(ggplot2)
df <- data.frame(x = as.Date("2020-01-01") + seq(0, 300, by = 30),
                 y = c(1.2, 2.1, 0.8, 3.5, 1.9, 2.7, 0.5, 1.1, 2.3, 1.6, 0.9))
ggplot(df, aes(x = x, y = y)) + geom_lolli3()

}
\keyword{datasets}
